{
  "name": "idb-manager",
  "version": "1.0.2",
  "description": "Una librería moderna y robusta para gestionar IndexedDB en el navegador",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "browser": "dist/cdn/index.js",
  "exports": {
    ".": {
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js",
      "types": "./dist/types/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "bun run build:dev && bun run serve",
    "build": "bun run clean && bun run build:types && bun run build:esm && bun run build:cjs && bun run build:cdn",
    "build:dev": "bun run clean && bun run build:esm",
    "build:types": "tsc --project tsconfig.build.json",
    "build:esm": "bun build src/index.ts --outdir dist/esm --format esm --target browser --sourcemap",
    "build:cjs": "bun build src/index.ts --outdir dist/cjs --format cjs --target node --sourcemap",
    "build:cdn": "bun run build:cdn:dev && bun run build:cdn:min",
    "build:cdn:dev": "bun build src/index.ts --outdir dist/cdn --format iife --target browser",
    "build:cdn:min": "bun build src/index.ts --outdir dist/cdn --format iife --target browser --minify --sourcemap",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "clean": "rimraf dist",
    "serve": "bun run --hot examples/server.ts",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts",
    "prepublishOnly": "bun run build && bun run test",
    "publish:npm": "npm publish --access public",
    "publish:dry": "npm publish --dry-run"
  },
  "keywords": [
    "indexeddb",
    "database",
    "browser",
    "typescript",
    "bun",
    "storage",
    "offline",
    "web"
  ],
  "author": "nglmercer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nglmercer/IDB_lib.git"
  },
  "bugs": {
    "url": "https://github.com/nglmercer/IDB_lib/issues"
  },
  "homepage": "https://github.com/nglmercer/IDB_lib#readme",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0"
  },
  "peerDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "browserslist": [
    "defaults",
    "not IE 11",
    "not op_mini all"
  ]
}